Test React application with redux addition. 

To start it: 
* Go to command line in project root directory and type `npm i` - to install all needed dependency
* `npm start` - to start application in development mode.
